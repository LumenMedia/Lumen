<ContentDialog
    x:Class="Lumen.Views.LoginDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Connect to Jellyfin"
    PrimaryButtonText="Sign in"
    CloseButtonText="Cancel"
    PrimaryButtonClick="SignIn_Click">
    <StackPanel Spacing="12" MinWidth="360">
        <TextBox x:Name="ServerBox" Header="Jellyfin server" PlaceholderText="https://jellyfin.example.com" />
        <TextBox x:Name="UsernameBox" Header="Username" />
        <PasswordBox x:Name="PasswordBox" Header="Password" />
        <InfoBar x:Name="ErrorBar" IsOpen="False" Severity="Error" />
        <ProgressRing x:Name="Progress" IsActive="False" HorizontalAlignment="Left" />
    </StackPanel>
</ContentDialog>
